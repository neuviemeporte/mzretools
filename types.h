#ifndef TYPES_H
#define TYPES_H

#include <cstdint>
#include <cstdlib>

using Byte = uint8_t;
using Word = uint16_t;
using Dword = uint32_t;
using Size = size_t;
using Offset = size_t;

#endif // TYPES_H
